# Arbor KTOR
Simple log implementation that passes through tags to cached Logger instances. If needed the Logger provider can be 
replaced for a custom implementation.

```kotlin
implementation("com.toxicbakery.logging:arbor-ktor:<version>")
```
